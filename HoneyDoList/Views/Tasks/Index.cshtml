@model List<HoneyDoList.Domain.Entities.Task>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<ul>
    @foreach (var task in Model)
    {
        <li>
            @task.Name
        </li>
    }
</ul>